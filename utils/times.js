const TIME = Object.freeze({
    MAX_LOGIN_ATTEMPTS: 3,
    OTP_RESEND_BLOCK_UNTIL: 1000 * 60 * 2,
    MAX_OTP_ATTEMPTS: 5,
    OTP_BLOCKED_UNTIL: 15 * 60 * 1000,
    INVITE_EMAIL_EXPIRY: 7 * 24 * 60 * 60 * 1000,
    ONBOARDING_SET_PASSWORD_EMAIL: 1000 * 60 * 15,
    AWS_PUT_OBJECT_URI_EXPIRY: 60 * 5,
    AWS_GET_OBJECT_URI_EXPIRY: 60 * 10,
    AWS_DOWNLOAD_OBJECT_URI_EXPIRY: 60 * 1,
});

module.exports = TIME;